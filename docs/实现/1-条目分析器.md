# 脚本 1 设计文档：条目分析器 `analyze_entries.py`

---

## 1. 输入参数

```bash
python analyze_entries.py --input <世界书路径.json> [--output <输出路径.json>]
```

| 参数 | 必需 | 默认值 | 说明 |
|---|---|---|---|
| `--input` | 是 | — | 世界书 JSON 文件路径 |
| `--output` | 否 | `{输入文件名}_analysis.json` | 分析结果输出路径 |

---

## 2. 输出 JSON 结构

```json
{
  "source": "学园都市0313.json",
  "generated_at": "2026-06-01T12:00:00",
  "entries": [
    {
      "uid": 5,
      "comment": "核心01：御坂美琴",
      "is_static": false,
      "is_mixed": true,
      "detected_markers": ["<% if", "getvar("],
      "suggested_split": true,
      "ejs_compound_ranges": [
        [5, 12]
      ],
      "split_boundaries": [
        {
          "type": "heading",
          "heading_level": 2,
          "heading_text": "人物介绍",
          "start_line": 0,
          "end_line": 4,
          "is_dynamic": false
        },
        {
          "type": "heading",
          "heading_level": 2,
          "heading_text": "关系状态",
          "start_line": 13,
          "end_line": 16,
          "is_dynamic": true
        }
      ]
    },
    {
      "uid": 8,
      "comment": "纯静态条目",
      "is_static": true,
      "is_mixed": false,
      "detected_markers": [],
      "suggested_split": false,
      "ejs_compound_ranges": null,
      "split_boundaries": null
    }
  ],
  "summary": {
    "total": 163,
    "static": 100,
    "dynamic": 41,
    "mixed": 22
  }
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| `source` | `string` | 源文件名 |
| `generated_at` | `string` | ISO 8601 时间戳 |
| `entries[].uid` | `int` | 条目的唯一 ID |
| `entries[].comment` | `string` | 条目标题（便于人工识别） |
| `entries[].is_static` | `bool` | 是否不含任何动态标记 |
| `entries[].is_mixed` | `bool` | 是否同时含静态和动态内容，且静态边界在 EJS 复合块外 |
| `entries[].detected_markers` | `string[]` | 命中的动态标记类型列表 |
| `entries[].suggested_split` | `bool` | 是否建议拆分 |
| `entries[].ejs_compound_ranges` | `[int, int][] \| null` | EJS 复合块的 `[start_line, end_line]` 行范围列表 |
| `entries[].split_boundaries` | `object[] \| null` | 拆分边界信息 |
| `summary.total` | `int` | entries 总数 |
| `summary.static` | `int` | 纯静态条目数 |
| `summary.dynamic` | `int` | 纯动态条目数 |
| `summary.mixed` | `int` | 混合条目数（含可拆分与不可拆分；不可拆分数包含在 mixed 中） |

### `split_boundaries` 子对象

| 字段 | 类型 | 说明 |
|---|---|---|
| `type` | `"heading" \| "xml" \| "ejs_block"` | 边界类型 |
| `heading_level` | `int \| null` | type=heading 时的层级（1=H1, 2=H2） |
| `heading_text` | `string \| null` | type=heading 时的标题文本 |
| `start_line` | `int` | 该段落起始行号（0-based） |
| `end_line` | `int` | 该段落结束行号（含） |
| `is_dynamic` | `bool` | 该段落是否包含动态标记 |

---

## 3. 处理流程

```
输入世界书.json
    │
    ▼
1. 备份原始文件 → {原名}.backup_{YYYYMMDD_HHMMSS}.json
    │
    ▼
2. load_world_book(input) → entries 字典
    │
    ▼
3. 遍历 entries，逐条目分析
    │
    ├─ 3a. detect_markers(content) → 动态标记列表
    ├─ 3b. parse_content_blocks(content) → text/ejs 块序列
    ├─ 3c. find_ejs_compound_ranges(blocks) → EJS 复合块行范围
    ├─ 3d. 三态判定（考虑 EJS 复合块覆盖率）
    ├─ 3e. 若 is_mixed → 检测拆分边界（排除复合块内边界）
    └─ 3f. 累积统计计数
    │
    ▼
4. 组装输出 JSON
    │
    ▼
5. 写入 {原名}_analysis.json
```

---

## 4. EJS 复合块识别（核心增强）

### 4.1 动机

EJS 的 `if/else/for/while` 结构跨越多个 `<% %>` → `%>` 交替块：

```text
<% if (cond) { %>        ← EJS块1, brace深度变为+1
  文本输出A
<% } else { %>           ← EJS块2, brace净变化=0 (深度仍为1)
  文本输出B
<% } %>                  ← EJS块3, brace深度-1=0（闭合）
```

如果机械按 `#` 标题或 `##` 标题切分，会割裂 EJS 结构，产生孤立的 `<% } %>` 语义残片。

### 4.2 数据结构

```python
from dataclasses import dataclass

@dataclass
class ContentBlock:
    type: str          # "text" | "ejs"
    content: str       # 块内容（不含 <% %> 标记）
    start_line: int    # 在原始 content 中的起始行号（0-based）
    end_line: int      # 在原始 content 中的结束行号（含）
    brace_delta: int   # 该 EJS 块的净 brace 变化（{数 - }数），text 块为 0
```

### 4.3 `parse_content_blocks(content) -> list[ContentBlock]`

将 content 按 `<%...%>` 边界切分为 text/ejs 交替序列。

**步骤**：
1. 按行分割 content
2. 扫描所有包含 `<%` 和 `%>` 的行，确定 EJS 块的起始/结束行
3. 跨行 EJS 块合并为单一块
4. 计算每个 EJS 块的 `brace_delta`（调用 `count_net_braces`）

**正则**：
- EJS 非输出块开始：`<%(?![-=])\s*`
- EJS 转义输出：`<%=`
- EJS 非转义输出：`<%-`

### 4.4 `count_net_braces(ejs_code: str) -> int`

统计 EJS 代码块中 `{` 和 `}` 的差值。

```python
def count_net_braces(code: str) -> int:
    """
    返回 { 数 - } 数。
    排除字符串字面量、正则表达式、模板字符串中的括号。
    """
    clean = _remove_string_literals(code)
    return clean.count("{") - clean.count("}")
```

### 4.5 `find_ejs_compound_ranges(content) -> list[tuple[int, int]]`

```python
def find_ejs_compound_ranges(content: str) -> list[tuple[int, int]]:
    """
    扫描所有 EJS 块，追踪 brace 深度。
    从深度 > 0 开始，到深度 == 0 结束，形成一个复合块。
    返回 [(start_line, end_line), ...]
    """
    blocks = parse_content_blocks(content)
    ranges = []
    depth = 0
    start = None

    for block in blocks:
        if block.type == "ejs":
            depth += block.brace_delta
            if depth > 0 and start is None:
                start = block.start_line
            if depth == 0 and start is not None:
                ranges.append((start, block.end_line))
                start = None

    return ranges
```

**示例**：

```
输入:
  line 0: # 状态总览
  line 1: <% if (cond) { %>
  line 2: # 阶段一事件
  line 3: 事件A
  line 4: <% } else { %>
  line 5: # 阶段二事件
  line 6: 事件B
  line 7: <% } %>

输出: [(1, 7)]
```

### 4.6 EJS 复合块对 `is_mixed` 的影响

```python
def classify(content: str) -> tuple[bool, bool]:
    """
    返回 (is_static, is_mixed)
    """
    markers = detect_markers(content)
    if not markers:
        return (True, False)  # 纯静态

    # 移除所有 EJS 块和宏，检查残留
    clean = _strip_ejs_and_macros(content).strip()
    if not clean:
        return (False, False)  # 纯动态

    # 有静态残留，检查是否被 EJS 复合块完全覆盖
    compound_ranges = find_ejs_compound_ranges(content)
    if compound_ranges and _content_fully_covered(lines(content), compound_ranges):
        return (False, False)  # 被复合块覆盖 → 不可拆分

    return (False, True)  # 混合可拆分
```

---

## 5. 动态标记检测

每类标记返回一个代表字符串（不重复计数）：

| # | 检测目标 | 正则模式 | 代表字符串 |
|---|---|---|---|
| 1 | EJS 非输出块 | `<%(?![-=])\s*` | `"<%"` |
| 2 | EJS 转义输出 | `<%=` | `"<%="` |
| 3 | EJS 非转义输出 | `<%-` | `"<%-"` |
| 4 | EJS `if` 条件 | `<%[-_]?\s*if\b` | `"<% if"` |
| 5 | getvar 宏 | `{{getvar::` | `"{{getvar::"` |
| 6 | setvar 宏 | `{{setvar::` | `"{{setvar::"` |
| 7 | incvar 宏 | `{{incvar::` | `"{{incvar::"` |
| 8 | xbgetvar 宏 | `{{xbgetvar_yaml_idx::` | `"{{xbgetvar_yaml_idx::"` |
| 9 | if 宏 | `{{if\b` | `"{{if"` |
| 10 | 实验性简写 (点) | `{{\.` | `"{{."` |
| 11 | 实验性简写 ($) | `{{\$` | `"{{$"` |
| 12 | getvar 函数 | `\bgetvar\(` | `"getvar("` |
| 13 | setvar 函数 | `\bsetvar\(` | `"setvar("` |
| 14 | variables 引用 | `\bvariables\.` | `"variables."` |
| 15 | 小白X state 块 | `<state>` | `"<state>"` |
| 16 | time 宏 | `{{time}}` | `"{{time}}"` |
| 17 | date 宏 | `{{date}}` | `"{{date}}"` |
| 18 | idleDuration 宏 | `{{idleDuration}}` | `"{{idleDuration}}"` |

> `{{char}}`、`{{user}}`、`{{model}}` 等名称类宏是静态宏，不在此列。`{{time}}`、`{{date}}`、`{{idleDuration}}` 每次展开值不同，属于动态宏。

---

## 6. 拆分边界检测

### 6.1 检测流程

```
1. find_ejs_compound_ranges(content) → 不可拆分行范围集合
2. 找到所有候选边界：
   - Markdown 标题行：^#{1,2}\s
   - XML 成对标签边界：<([\u4e00-\u9fff\w]+)>...</\1>
3. 过滤：若标题段落的 start 或 end 行号落在任一 EJS 复合块区间内：
   - 单行段落且全部在复合块内 → 保留，按内容判定 is_dynamic
   - 跨边界段落（部分在复合块内、部分在外）→ 保留，强制标记 is_dynamic=True（保护 EJS 完整性）
4. 剩余边界作为拆分点，切分 content 为段落
5. 每段落判定 is_dynamic：
   - 段落文本匹配任何动态标记 → true
   - 否则 → false
```

### 6.2 过滤示意

```text
场景:
  line 0: # 状态总览          ← 候选边界, 复合块外 → 保留
  line 1: <% if (cond) { %>   ← EJS复合块开始: lines 1-7
  line 2: # 阶段一事件         ← 候选边界, 在复合块内 → 丢弃!
  line 3: 事件A的描述
  line 4: <% } else { %>
  line 5: # 阶段二事件         ← 候选边界, 在复合块内 → 丢弃!
  line 6: 事件B的描述
  line 7: <% } %>             ← EJS复合块闭合
  line 8: # 另一个标题         ← 候选边界, 复合块外 → 保留

最终拆分点: line 0, line 8
段落1: lines 0-7 (含整个 EJS 复合块 → is_dynamic=true)
段落2: lines 8+  (...)
```

---

## 7. 特殊分支处理

| 场景 | 处理方式 |
|---|---|
| **空 content** (`""`) | `is_static=true, is_mixed=false`, markers=[], boundaries=null |
| **仅有空白** (`"\n \t"`) | 同空 content |
| **outlet 条目** (`position=7`) | `is_mixed=false, suggested_split=false`，不参与拆分、重排序、合并 |
| **插件标记条目** (`[GENERATE:BEFORE]`/`[InitialVariables]`/`@@preprocessing` 等) | `is_mixed=false, suggested_split=false`，不参与拆分 |
| **disabled 条目** (`disable=true`) | 正常分析，不跳过（disable 在后续脚本中处理） |
| **静态宏**（`{{char}}`、`{{user}}` 等） | 不在检测列表 → `is_static=true` |
| **纯 EJS 全覆盖** | `is_mixed=false`（clean 后无残留） |
| **EJS 复合块全覆盖** | `is_mixed=false, suggested_split=false` |
| **EJS 复合块未闭合** | 标记为不可拆分（`suggested_split=false`），保留用户手动处理空间 |
| **嵌套 EJS 复合块** | 按最外层 brace 归零判定范围；内层复合块列表仍记录在 `ejs_compound_ranges` |
| **`for` / `while` 块** | brace 跟踪统一处理，不区分关键字类型 |
| **混合不可拆分** | `is_mixed=true, suggested_split=false`（如无有效拆分边界或残留全在复合块内） |
| **entries 为空字典 `{}`** | 不报错，输出 entries=[], summary 全 0 |
| **JSON 解析失败** | 报错退出 |
| **文件不存在** | 报错退出 |

---

## 8. 依赖关系

```
world_book_utils.py:
  - load_world_book()
  - save_world_book()
  - backup_file()
  - detect_markers()
  - classify_entry()          # 综合三态判定
  - parse_content_blocks()    # 切分 text/ejs 块序列
  - find_ejs_compound_ranges() # EJS 复合块行范围
  - count_net_braces()        # 统计 brace 净值
  - has_special_plugin()      # 检测插件标记
  - is_outlet_entry()         # 检测 outlet 条目
  - find_xml_tags()
  - split_by_headings()
  - _is_line_in_ranges()      # 判断行号是否在复合块区间内
  - _content_fully_covered()   # 检查 content 是否被复合块完全覆盖
  - _strip_ejs_and_macros()    # 移除所有 EJS 块和宏
  - _remove_string_literals()  # 移除 JavaScript 字符串字面量
```

---

## 9. API 函数签名

```python
def run(input_path: str, output_path: str | None = None) -> str:
    """
    执行条目分析。

    Args:
        input_path: 世界书 JSON 文件路径
        output_path: 输出路径，None 时自动生成为 {input_stem}_analysis.json

    Returns:
        分析结果 JSON 文件的路径
    """
```

---

## 10. 使用示例

```bash
# 基本用法（输出到同目录 {原名}_analysis.json）
python analyze_entries.py --input 学园都市0313.json

# 指定输出路径
python analyze_entries.py --input 学园都市0313.json --output result/analysis.json
```

```python
from analyze_entries import run

analysis_path = run("学园都市0313.json")
# → "学园都市0313_analysis.json"
```
