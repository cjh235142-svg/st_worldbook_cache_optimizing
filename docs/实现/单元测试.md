# 单元测试方案

## 测试框架

- **pytest** + 本地 JSON fixture 文件
- 测试目录 `scripts/tests/`，与源码目录 `scripts/` 平行

---

## 文件结构

```
scripts/
├── tests/
│   ├── __init__.py
│   ├── conftest.py                  # 共享 fixture 加载
│   ├── fixtures/
│   │   ├── static_entry.json        # 纯静态：普通文本 + XML 标签
│   │   ├── dynamic_entry.json       # 纯动态：EJS + getvar + if 全覆盖
│   │   ├── mixed_entry.json         # 混合可拆分：标题分界 + XML 包裹
│   │   ├── mixed_unsplittable.json  # 混合不可拆分：纯 EJS 条件全覆盖
│   │   ├── multi_position.json      # 7 条 position=0~6 各一
│   │   ├── multi_depth.json         # 5 条 depth=0,1,9,10,9999
│   │   ├── multi_order_static.json  # 5 条同层同 pos 不同 order（静态）
│   │   ├── multi_order_dynamic.json # 5 条动态不同 pos
│   │   ├── small_world_book.json    # 综合：3 静态 + 2 动态 + 1 混合
│   │   ├── empty_entries.json       # entries 为空 {}
│   │   ├── single_entry.json        # 单条目（边界）
│   │   └── legacy_fields.json       # 含 null 字段 scanDepth/role 等
│   ├── test_utils.py                # world_book_utils 单元测试
│   ├── test_analyze.py              # 脚本1 单元测试
│   ├── test_split.py                # 脚本2 单元测试
│   ├── test_reorder.py              # 脚本3 单元测试
│   └── test_merge.py                # 脚本4 单元测试
├── world_book_utils.py
├── analyze_entries.py
├── split_entries.py
├── reorder_entries.py
├── merge_entries.py
└── run_pipeline.py
```

---

## 测试配置 `conftest.py`

```python
import pytest
import json
from pathlib import Path

FIXTURES_DIR = Path(__file__).parent / "fixtures"


def load_fixture(name: str) -> dict:
    """加载 fixtures 目录下的 JSON 文件"""
    with open(FIXTURES_DIR / name, "r", encoding="utf-8") as f:
        return json.load(f)


@pytest.fixture
def fixtures_dir() -> Path:
    return FIXTURES_DIR


@pytest.fixture
def tmp_world_book_path(tmp_path) -> Path:
    """用于测试写入的临时文件路径"""
    return tmp_path / "test_world_book.json"
```

---

## `test_utils.py` — 公用函数 (50 用例)

### `is_static()` / `detect_markers()` (20 用例)

| # | 用例 | 输入 content | 预期 is_static | 预期 markers |
|---|---|---|---|---|
| 1 | 纯文本静态 | `"这是普通描述文本"` | `True` | `[]` |
| 2 | 仅XML标签静态 | `"<角色>设定</角色>"` | `True` | `[]` |
| 3 | EJS代码块动态 | `"<% if(...) { %>"` | `False` | `["<%"]` |
| 4 | EJS输出动态 | `"<%= var %>"` | `False` | `["<%="]` |
| 5 | EJS未转义输出动态 | `"<%- var %>"` | `False` | `["<%-"]` |
| 6 | getvar宏动态 | `"{{getvar::x}}"` | `False` | `["{{getvar::"]` |
| 7 | setvar宏动态 | `"{{setvar::x::y}}"` | `False` | `["{{setvar::"]` |
| 8 | incvar宏动态 | `"{{incvar::x}}"` | `False` | `["{{incvar::"]` |
| 9 | xbgetvar宏动态 | `"{{xbgetvar_yaml_idx::x}}"` | `False` | `["{{xbgetvar_yaml_idx::"]` |
| 10 | if宏动态 | `"{{if condition}}"` | `False` | `["{{if"]` |
| 11 | 点前缀简写动态 | `"{{.myvar}}"` | `False` | `["{{."]` |
| 12 | 美元简写动态 | `"{{$global}}"` | `False` | `["{{$"]` |
| 13 | getvar函数动态 | `"getvar('x')"` | `False` | `["getvar("]` |
| 14 | setvar函数动态 | `"setvar('x')"` | `False` | `["setvar("]` |
| 15 | variables引用动态 | `"variables.x"` | `False` | `["variables."]` |
| 16 | 多标记同时命中 | `"<% ... %> {{getvar::x}}"` | `False` | `["<%", "{{getvar::"]` |
| 17 | 空字符串 | `""` | `True` | `[]` |
| 18 | 仅换行空白 | `"\n\n"` | `True` | `[]` |
| 19 | 相似但非宏 | `"{{char}}"` | `True` | `[]` |
| 20 | 大括号无宏 | `"{ something }"` | `True` | `[]` |

### `is_mixed()` (4 用例)

| # | 用例 | 输入 | 预期 |
|---|---|---|---|
| 21 | 静态+动态混合 | 部分静态文本 + 部分 `<%...%>` | `True` |
| 22 | 纯静态 | 全文本无标记 | `False` |
| 23 | 纯动态 | 整段在 `<%...%>` 内 | `False` |
| 24 | 空字符串 | `""` | `False` |

### `find_xml_tags()` (6 用例)

| # | 用例 | 输入 | 预期 |
|---|---|---|---|
| 25 | 单个标签对 | `<角色>内容</角色>` | 返回1对，标签名="角色" |
| 26 | 无标签 | 纯文本 | 返回空 |
| 27 | 嵌套标签 | `<A><B>x</B></A>` | 识别嵌套，最外层="A" |
| 28 | 多个同级标签 | `<X>x</X><Y>y</Y>` | 返回2对 |
| 29 | 未闭合标签 | `<X>内容` | 不识别或标记无效 |
| 30 | 自闭合标签 | `<X/>` | 不匹配成对检测 |

### `split_by_headings()` (5 用例)

| # | 用例 | 输入 | 预期段落数 |
|---|---|---|---|
| 31 | H1分界 | `# A\nx\n# B\ny` | 2 |
| 32 | H2分界 | `## A\nx\n## B\ny` | 2 |
| 33 | H1+H2混排 | `# H1\nx\n## H2\ny\n# H1\nz` | 3（按H1层级） |
| 34 | 无标题 | 纯文本 | 1 |
| 35 | 标题+空段落 | `# A\n# B` | 2 |

### `reassign_uids()` (3 用例)

| # | 用例 | 输入 | 预期 |
|---|---|---|---|
| 36 | 3条正常 | uid=5,12,3 | uid=0,1,2 |
| 37 | 空entries | `{}` | `{}` |
| 38 | 单条 | uid=99 | uid=0 |

### `sort_entries()` (5 用例)

| # | 用例 | 输入 | 预期顺序 |
|---|---|---|---|
| 39 | 静态全前 | 2静态+2动态 | 静态→静态→动态→动态 |
| 40 | 同静态pos升序 | pos=1, pos=0 均静态 | pos=0 在前 |
| 41 | 同pos同静态depth降序 | depth=5,10,9999 均静态 | 9999→10→5 |
| 42 | 完全同条件order维持 | order=8,3 均静/同pos/同depth | 3→8 |
| 43 | 综合排序 | 6条覆盖各情况 | 按 `(is_static, pos, -depth, order)` 升序 |

### `load_world_book()` / `save_world_book()` / `backup_file()` (7 用例)

| # | 用例 | 输入 | 预期 |
|---|---|---|---|
| 44 | 加载合法JSON | 有效文件 | 返回 dict，含 entries |
| 45 | 加载无entries | `{}` | 返回 `{"entries": {}}` |
| 46 | 加载不存在文件 | 错误路径 | 报错 |
| 47 | 加载非法JSON | 损坏文件 | 报错 |
| 48 | 保存写入 | entries+路径 | 文件存在且内容一致 |
| 49 | 备份创建 | 存在文件 | `backup_时间戳.json` 内容一致 |
| 50 | 备份目录不存在 | -- | 自动创建目录后备份 |

---

## `test_analyze.py` — 脚本1 (9 用例)

| # | 用例 | fixture/输入 | 预期 |
|---|---|---|---|
| 51 | 纯静态条目分析 | `static_entry.json` | 所有条目 `is_static=true is_mixed=false` |
| 52 | 纯动态条目分析 | `dynamic_entry.json` | `is_static=false is_mixed=false` |
| 53 | 混合可拆分条目分析 | `mixed_entry.json` | `is_static=false is_mixed=true suggested_split=true` |
| 54 | 混合不可拆分不建议拆分 | `mixed_unsplittable.json` | `is_mixed=true suggested_split=false` |
| 55 | disabled条目也分析 | 条目 `disable=true` | 正常分析，不被跳过 |
| 56 | 输出JSON结构完整 | `small_world_book.json` | 含 source / generated_at / entries / summary |
| 57 | summary统计正确 | 1静+1动+1混 | `{total:3, static:1, dynamic:1, mixed:1}` |
| 58 | 全静态summary | `static_entry.json` | `{total:N, static:N, dynamic:0, mixed:0}` |
| 59 | 备份创建 | 任意输入 | 原文件已生成备份 |

---

## `test_split.py` — 脚本2 (13 用例)

| # | 用例 | fixture/输入 | 预期 |
|---|---|---|---|
| 60 | 按H2标题拆分 | mixed 含 `## A\n静\n## B\n<%动%>` | 拆为静态+动态各一条 |
| 61 | XML标签保留 | `<角色>...<%...%>...</角色>` | 拆分后双方各裹 `<角色>...</角色>` |
| 62 | 拆分语义等价 | -- | 拆分后静态+动态 content 拼接 ≈ 拆前 content |
| 63 | 纯静态不拆分 | `static_entry.json` | 原样保留 |
| 64 | 纯动态不拆分 | `dynamic_entry.json` | 原样保留 |
| 65 | suggested_split=false 跳过 | analysis中设为false | 不拆分，原样保留 |
| 66 | 全EJS覆盖不可拆分 | `mixed_unsplittable.json` | 原样保留 |
| 67 | comment后缀[split-static] | -- | 静态子条目 comment 包含 `[split-static]` |
| 68 | comment后缀[split-dynamic] | -- | 动态子条目 comment 包含 `[split-dynamic]` |
| 69 | key继承 | 原条目 key=["k1","k2"] | 子条目 key=["k1","k2"] |
| 70 | keysecondary继承 | 原条目 keysecondary=["k3"] | 子条目 keysecondary=["k3"] |
| 71 | uid重分配连续 | 含拆分操作 | 最终 uid=0,1,2,... |
| 72 | 空entries不报错 | `empty_entries.json` | 正常结束 |

---

## `test_reorder.py` — 脚本3 (21 用例)

### 字段修改 — 静态 (7 用例)

| # | 用例 | 输入条件 | 预期修改 |
|---|---|---|---|
| 73 | 静态 pos4 depth<10 | `pos=4 depth=5 static` | `pos=4 depth=0 role=1 constant=true cooldown=0 probability=100 useProbability=false sticky=0 delay=0` |
| 74 | 静态 pos4 depth=10 | `pos=4 depth=10 static` | `depth=9999`（其余同#73） |
| 75 | 静态 pos4 depth=99 | `pos=4 depth=99 static` | `depth=9999`（≥10边界） |
| 76 | 静态 pos!=4 (pos=0) | `pos=0 static` | pos不变, depth不变, `constant=true cooldown=0 probability=100 useProbability=false sticky=0 delay=0` |
| 77 | 静态 pos!=4 (pos=1) | `pos=1 static` | 同#76 |
| 78 | 静态 pos!=4 (pos=6) | `pos=6 static` | 同#76 |
| 79 | 静态 null 字段不动 | `scanDepth=null role=null` | `scanDepth=null role=null`（不改动） |

### 字段修改 — 动态 (4 用例)

| # | 用例 | 输入条件 | 预期修改 |
|---|---|---|---|
| 80 | 动态原pos=0 | `pos=0 depth=N dynamic` | `pos=4 depth=0 role=1`, constant等保持原值 |
| 81 | 动态原pos=4 | `pos=4 depth=5 dynamic` | `pos=4 depth=0 role=1`, 其他保持原值 |
| 82 | 动态 constant=false 保持 | 动态条目 `constant=false` | `constant=false`（不变） |
| 83 | 动态 cooldown/概率 保持原值 | 动态条目 `cooldown=5 probability=50` | `cooldown=5 probability=50`（不变） |

### 排序与 order 分配 (10 用例)

| # | 用例 | 输入 | 预期 |
|---|---|---|---|
| 84 | 静态全在动态前 | 3静态+3动态 | 排序后前3为静态 |
| 85 | 同层pos升序 | 静态 pos=1,0,4 | pos→0,1,4 |
| 86 | 同pos depth降序 | 静态 pos=4 depth=5,10,9999 | depth=9999,10,5 |
| 87 | 同条件order保序 | 静态同pos同depth order=3,8 | 3→8 |
| 88 | order连续重分配 | 全静态5条 | new_order=0,1,2,3,4 |
| 89 | 动态offset正确 | 2静态+2动态 | min(动态.order) > max(静态.order) |
| 90 | 动态offset值正确 | 静态max_order=3 | 动态最小order >= 4 |
| 91 | 纯静态无offset | 无动态条目 | 不报错, order从0连续 |
| 92 | 纯动态offset计算 | 无静态条目（offset=0+1） | 动态order从1起 |
| 93 | 原constant=true静态保持 | 原已是蓝灯静态 | `constant=true`（保持） |

### 边界条目检测 (7 用例)

| # | 用例 | 输入 content | 预期 |
|---|---|---|---|
| 94 | XML 开边界—纯标签 | `"<暗部>"` | `is_boundary=True, type="xml_open", name="暗部"` |
| 95 | XML 闭边界—纯标签 | `"</暗部>"` | `is_boundary=True, type="xml_close", name="暗部"` |
| 96 | XML 开边界—带描述文本 | `"<暗部>\n暗部组织介绍"` | `is_boundary=True, copy_content="<暗部>"`（仅标签） |
| 97 | Markdown 开边界 | `"# 暗部开始"` | `is_boundary=True, type="md_open", name="暗部"` |
| 98 | Markdown 闭边界 | `"## 暗部结束"` | `is_boundary=True, type="md_close", name="暗部"` |
| 99 | 非边界—普通已闭合条目 | `"<暗部>成员设定</暗部>"` | `is_boundary=False` |
| 100 | 非边界—含动态标记 | `"<暗部>\n<% getvar() %>"` | `is_boundary=False`（有动态标记） |

### 边界配对 (4 用例)

| # | 用例 | 输入 | 预期 |
|---|---|---|---|
| 101 | 简单配对 | 1个 `<A>` + 1个 `</A>` 同 position | 配出 1 对 |
| 102 | 无配对—仅有开标签 | `<A>` 无对应 `</A>` | 不生成副本 |
| 103 | 嵌套配对 | `<A>` `<B>` `</B>` `</A>` 同 position | 配出 2 对 |
| 104 | 跨 position 不配对 | `<A>` pos=0, `</A>` pos=1 | 不配对 |

### 边界副本创建与属性 (3 用例)

| # | 用例 | 输入条件 | 预期 |
|---|---|---|---|
| 105 | 区间含动态 → 复制 | 边界间有 dynamic 条目 | 创建开+闭副本，`_is_boundary_copy=True` |
| 106 | 区间全静态 → 不复制 | 边界间全 static | 不创建副本 |
| 107 | 副本属性 | 复制后的边界副本 | `content` 仅含标签/标题行，`comment` 含 `[boundary-copy-open]` |

### 补充包裹生成 (4 用例)

| # | 用例 | 输入条件 | 预期 |
|---|---|---|---|
| 108 | XML 风格检测 | 世界书 XML 标签多于 MD 标题 | 包裹内容为 `<补充内容>` / `</补充内容>` |
| 109 | MD 风格检测 | MD 标题多于 XML 标签 | 包裹内容为 `# 补充内容开始` / `# 补充内容结束` |
| 110 | 补充包裹属性 | 创建后包裹条目 | `constant=false, key=["/.*/"], _is_supplement=True` |
| 111 | `--wrapper-name` 覆盖 | `wrapper_name="可变内容"` | 包裹内容用 `"可变内容"` 替代默认 |

### 排序—边界副本与补充包裹 (2 用例)

| # | 用例 | 输入条件 | 预期 |
|---|---|---|---|
| 112 | 副本/包裹归入 category=1 | 边界副本 + 纯静态条目 | 副本排在所有纯静态之后（与动态同层） |
| 113 | 开副本在动态首、闭副本在动态尾 | 一对边界副本 + 若干动态 | order: 开 < 动态 < 闭 |

### 去空 (2 用例)

| # | 用例 | 输入条件 | 预期 |
|---|---|---|---|
| 114 | 去空—无动态时删除副本 | 排序后边界对区间内无动态条目 | 边界副本对被删除 |
| 115 | 去空—无动态时删除补充包裹 | 世界书无任何动态条目 | `_is_supplement` 条目全部删除 |

---

## `test_merge.py` — 脚本4 (15 用例)

| # | 用例 | fixture/输入 | 预期 |
|---|---|---|---|
| 94 | 单组合并 | 同pos/depth/constant 3条静态 | 合并为1条 |
| 95 | 多组分别合并 | 两组不同depth | 各组合并为1条 |
| 96 | 不同constant不合并 | 同pos depth但constant不同 | 不合并到同组 |
| 97 | content `\n\n` 拼接 | "A","B","C" | `"A\n\nB\n\nC"` |
| 98 | content按order升序排 | order=5,1,3 | 拼接顺序 1→3→5 |
| 99 | order取最小 | order=5,1,3 | 合并后order=1 |
| 100 | comment格式 | uid=3,7,12 | `"合并:3-12"` |
| 101 | key清空 | 原key=["a"] | `[]` |
| 102 | keysecondary清空 | 原keysecondary=["b"] | `[]` |
| 103 | 其他字段取默认值 | -- | 验证 comment/key 外的字段均为默认值 |
| 104 | 动态条目完全不变 | 独立动态条目 | 与输入一致 |
| 105 | 动态不与静态合并 | 动态+静态同pos同depth | 分开两条，动态不并入 |
| 106 | 单条目不合并不拼多余换行 | 组内仅1条 | content不变（无多余`\n\n`） |
| 107 | uid最终连续 | 合并后 | uid=0,1,2,... |
| 108 | 空entries不报错 | `empty_entries.json` | 正常结束 |

### 合并排除—边界副本与补充包裹 (3 用例)

| # | 用例 | 输入条件 | 预期 |
|---|---|---|---|
| 109 | 边界副本不参与静态合并 | `<暗部>` 副本 + 静态条目同 `(pos,constant,depth)` | 副本不与静态合并到同一组 |
| 110 | 补充包裹不参与静态合并 | `<补充内容>` + 静态条目同 `(pos,constant,depth)` | 包裹不与静态合并到同一组 |
| 111 | 副本/包裹归入 non_mergeable | `is_mergeable_static()` 输入副本/包裹 | 返回 False |

---

## 汇总

| 测试文件 | 用例数 |
|---|---|
| `test_utils.py` | 50 |
| `test_analyze.py` | 9 |
| `test_split.py` | 13 |
| `test_reorder.py` | 43 |
| `test_merge.py` | 18 |
| **总计** | **133** |
